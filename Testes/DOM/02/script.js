let el = document.getElementsByClassName("classe");

for (let i = 0; i < el.length; i++) { // podemos acessar pelo indice
    console.log(el[i]);
    console.log(el[i].textContent); // podemos acessar atributos
}

